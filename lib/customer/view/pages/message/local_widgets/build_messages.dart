import 'package:flutter/material.dart';

class BuildMessages extends StatelessWidget {
  const BuildMessages({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}